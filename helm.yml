appName: catalogue
service:
  port: 8080
  type: ClusterIP
ingress:
  enabled: false
  scheme: null

configMap:
  enabled: true
  data:
    DOCUMENTDB: "true"

externalSecret:
  enabled: true

hpa:
  min: 2
  max: 10
  cpuAvg: 80

